{
  "name": "@kainoa/simplybuild",
  "version": "0.1.1",
  "description": "Smart iOS build and deploy CLI wrapper for xcodebuildmcp",
  "type": "module",
  "bin": {
    "simplybuild": "./dist/cli.js",
    "sb": "./dist/cli.js",
    "simplebuild": "./dist/cli.js",
    "simple-build": "./dist/cli.js",
    "simply-build": "./dist/cli.js",
    "symplebuild": "./dist/cli.js",
    "symplybuild": "./dist/cli.js",
    "symple-build": "./dist/cli.js",
    "symply-build": "./dist/cli.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "keywords": [
    "ios",
    "xcode",
    "xcodebuild",
    "cli",
    "xcodebuildmcp"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@clack/prompts": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2",
    "vitest": "^3.2.4"
  }
}
